# Independent replication request

This request is intentionally public. The goal is for an outside researcher to reproduce at least H1 and H4 from the published manifest, including treatment-minus-control means and confidence intervals.

Please report the release/commit, Python version, OS, exact command, result hashes, and deviations.

The automated `reproduce.py` check is not an independent human replication. This remains open until an outside researcher reproduces the results independently.
